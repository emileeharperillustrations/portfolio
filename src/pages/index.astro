---
import { Image } from "astro:assets";
import { getCollection } from "astro:content";
import MainLayout from "@layouts/MainLayout.astro";
import Container from "@components/layout/Container.astro";
import HeroImg from "@assets/Woodland_Reflections.jpg";

const portfolio = await getCollection("portfolio");
---

<style>
  .artwork {
    display: block;
    width: 100%;
    height: auto;
    aspect-ratio: 13 / 17;
    object-fit: cover;
    object-position: center;
  }

  .art-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(var(--min-art-width), 1fr));
    gap: 1rem;
  }

  .hero {
    display: none;
    position: relative;
  }

  .desktop-title {
    display: none;
    position: relative;
  }

  @media (width >= 65rem) {
    .hero {
      display: block;
      width: 100%;
      height: auto;
      aspect-ratio: 24 / 9;
      object-fit: cover;
      object-position: center;
      background-repeat: no-repeat;
    }

    .desktop-title {
      display: block;
      position: fixed;
      top: 0;
      left: 0;
      right: 0;
      padding-top: 0.5rem;
    }

    .inner-desktop-title {
      background-color: black;
      padding: 0.25rem 1.1rem;
      border-radius: 21px;

      max-width: fit-content;
      margin: auto;
    }
  }
</style>

<MainLayout title="Portfolio" currentPage="portfolio">
  <Image id="hero" src={HeroImg} alt="" class="hero" layout="full-width" />

  <div id="desktop-title" data-sticky-title="true" class="desktop-title title">
    <div id="inner-desktop-title" class="inner-desktop-title">
      Emilee Harper
    </div>
  </div>
  <Container size="lg">
    <div class="art-grid">
      {
        portfolio
          .sort(
            (a, b) =>
              new Date(b.data.date).getTime() - new Date(a.data.date).getTime(),
          )
          .map((artwork) => (
            <Image
              src={artwork.data.image}
              layout="full-width"
              alt={artwork.data.alt}
              class="artwork"
            />
          ))
      }
    </div>
  </Container>
</MainLayout>
