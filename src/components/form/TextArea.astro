---
import type { HTMLAttributes } from "astro/types";

interface Props extends HTMLAttributes<"textarea"> {
  label?: String;
}
const { label: labelText, ...attrs } = Astro.props;
---

<div class="pb-2">
  <label for={attrs.id}>{labelText}</label>
  <textarea {...attrs}></textarea>
</div>
